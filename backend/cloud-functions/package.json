{
  "name": "padiguard-functions",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "echo \"No lint configured\"",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@turf/turf": "^7.3.4",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0"
  }
}
