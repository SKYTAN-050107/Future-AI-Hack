rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hackathon-safe development rule. Tighten before production launch.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
